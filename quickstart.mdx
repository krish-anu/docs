---
title: "Quickstart"
description: "Get started with Trash2Treasure by setting up the platform locally in under 5 minutes"
---

## Prerequisites

<Info>
  Before you begin, make sure you have the following installed: - **Node.js**
  v18 or higher - **npm** or **yarn** - **Docker** and **Docker Compose** (for
  database) - **Git**
</Info>

## Quick Setup

<Steps>
<Step title="Clone the Repository">

```bash
git clone https://github.com/trash2treasure/trash2treasure.git
cd trash2treasure
```

</Step>

<Step title="Start the Database">

Start PostgreSQL using Docker Compose:

```bash
docker-compose -f docker-compose.dev.yml up -d
```

This will start:

- PostgreSQL on port `5432`
  </Step>

<Step title="Set Up the Server">

```bash
cd server
cp .env.example .env
npm install
npx prisma migrate dev
npx prisma db seed
npm run start:dev
```

The API server will be running at `http://localhost:4000`

</Step>

<Step title="Set Up the Client">

Open a new terminal:

```bash
cd client
npm install
npm run dev
```

The client will be running at `http://localhost:3000`

</Step>
</Steps>

## Default Test Accounts

After seeding the database, you can use these accounts to explore the platform:

| Role   | Email                    | Password     |
| ------ | ------------------------ | ------------ |
| Admin  | admin@trash2treasure.lk  | Password123! |
| User   | user@trash2treasure.lk   | Password123! |
| Driver | driver@trash2treasure.lk | Password123! |

## Project Structure

```
trash2treasure/
├── client/          # Next.js frontend
│   ├── app/         # App router pages
│   ├── components/  # React components
│   └── lib/         # Utilities and API client
├── server/          # NestJS backend
│   ├── src/         # Source code
│   └── prisma/      # Database schema and migrations
└── docker-compose.yml
```

## Next Steps

Once set up, you can book a pickup by logging in as a user and navigating to the bookings section.

<CardGroup cols={2}>
  <Card title="Architecture" icon="sitemap" href="/architecture">
    Understand the system architecture and data flow.
  </Card>
  <Card title="Client Guide" icon="react" href="/client/overview">
    Learn about the Next.js frontend structure.
  </Card>
  <Card title="Server Guide" icon="server" href="/server/overview">
    Explore the NestJS backend modules.
  </Card>
  <Card
    title="API Reference"
    icon="terminal"
    href="/api-reference/introduction"
  >
    Browse the complete API documentation.
  </Card>
</CardGroup>
